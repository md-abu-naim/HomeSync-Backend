enum Role {
  OWNER
  TENANT
  ADMIN
}

enum AuthProvider {
  CREDENTIAL
  GOOGLE
}

enum UserStatus {
  ACTIVE
  BLOCKED
  DELETED
}

enum PropertyType {
  APARTMENT
  HOUSE
  HOSTEL
  ROOM
}

enum PropertyStatus {
  ACTIVE
  INACTIVE
}

enum RoomType {
  SINGLE
  DOUBLE
  SHARED
}

enum RoomAvailability {
  AVAILABLE
  RESERVED
  OCCUPIED
}

enum Gender {
  MALE
  FEMALE
  OTHER
}

enum VerificationStatus {
  PENDING
  VERIFIED
  REJECTED
}

enum ApplicationStatus {
  PENDING
  APPROVED
  REJECTED
  CANCELLED
}

enum PaymentProvider {
  BKASH
  STRIPE
  SSLCOMMERZ
}

enum PaymentStatus {
  UNPAID
  PAID
  FAILED
  CANCELLED
  REFUNDED
}

enum PaymentType {
  RENT
  SECURITY_DEPOSIT
  BOOKING_FEE
}

enum SleepSchedule {
  EARLY_BIRD
  NIGHT_OWL
  FLEXIBLE
}
